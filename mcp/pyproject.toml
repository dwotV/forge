[project]
name = "forge-mcp"
version = "1.0.0"
description = "Forge MCP Server — pentesting environment tools for LLMs"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.9.0",
    "httpx>=0.28.0",
]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
